import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20,
  },
  textInput: {
    fontSize: 18,
    backgroundColor: '#fff',
    paddingLeft: 10,
  },
});

export default styles;
